apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: autospec-web
  namespace: semute
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: autospec-web
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 75

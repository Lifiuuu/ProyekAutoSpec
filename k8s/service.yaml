apiVersion: v1
kind: Service
metadata:
  name: autospec-web
  namespace: semute
  labels:
    app.kubernetes.io/name: proyekautospec
    app.kubernetes.io/component: web
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: proyekautospec
    app.kubernetes.io/component: web
  ports:
    - name: http
      port: 80
      targetPort: 8000

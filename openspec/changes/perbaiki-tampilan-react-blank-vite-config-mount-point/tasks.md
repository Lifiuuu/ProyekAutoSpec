# Tasks

1. Update Blade: add `@viteReactRefresh` and change `resources/js/app.js` → `resources/js/app.jsx`.
2. Change HTML mount `id` from `root` → `app`.
3. Add `resources/js/app.jsx` that imports bootstrap/client modules and mounts `AppLayout` to `#app`.
4. Verify in development: run `npm run dev` (or `pnpm`/`yarn`) and visit the page to confirm React renders and hot refresh works.

name: configure-auth-tailwind
title: Configure Tailwind and mount `Auth.jsx` into `App.jsx`
description: |
  Configure Tailwind color tokens for AutoSpec (`dark-bg`, `primary-blue`),
  create an `App.jsx` entry that mounts the existing `Auth.jsx` with the
  required background, and verify Vite env vars `VITE_SUPABASE_URL` and
  `VITE_SUPABASE_ANON_KEY` are read by the frontend.
artifacts:
  - proposal
  - design
  - tasks
apply:
  requires:
    - tasks
applied: true
appliedAt: 2026-04-08T00:00:00Z

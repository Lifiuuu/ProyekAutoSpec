Change `add-auth-component` applied

Summary:
- Implemented `src/lib/supabaseClient.js` and `resources/js/lib/supabaseClient.js` (Supabase client)
- Implemented React `Auth` component at `src/components/Auth.jsx` and `resources/js/components/Auth.jsx`
- Mounted component in `resources/js/app.js` and added test page `resources/views/auth.blade.php`
- Added route `/auth-test` in `routes/web.php`
- Updated `package.json` dependencies and installed node modules
- Updated `README.md` with setup and test instructions
- Marked all tasks complete in `tasks.md`

If you want this change archived, run `/opsx:archive add-auth-component`.
